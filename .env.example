# Database
DATABASE_URL=postgresql://promptstack:promptstack@localhost:5432/promptstack

# JWT Configuration
AUTH_JWT_SECRET_KEY=your-secret-key-change-in-production
AUTH_JWT_ALGORITHM=HS256
AUTH_JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Origins (comma-separated)
AUTH_API_CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8000

# Dev Mode Settings
AUTH_ALLOW_DEV_HEADERS=true

# OIDC (optional - for production)
# AUTH_OIDC_ISSUER=https://cognito-idp.region.amazonaws.com/poolid
# AUTH_OIDC_AUDIENCE=client-id
# AUTH_OIDC_JWKS_URL=https://cognito-idp.region.amazonaws.com/poolid/.well-known/jwks.json
# Database
DATABASE_URL=postgresql://promptstack:promptstack@localhost:5432/promptstack

# Auth Service URL (for service-to-service communication)
API_AUTH_SERVICE_URL=http://localhost:8001

# CORS Origins (comma-separated)
API_API_CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:3001,http://localhost:8001

# Dev Mode Settings
API_ALLOW_DEV_HEADERS=true

# OIDC (optional - for production)
# API_OIDC_ISSUER=https://cognito-idp.region.amazonaws.com/poolid
# API_OIDC_AUDIENCE=client-id
# API_OIDC_JWKS_URL=https://cognito-idp.region.amazonaws.com/poolid/.well-known/jwks.json

